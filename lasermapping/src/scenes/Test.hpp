
#pragma once

#include "components/Scenes.h"
#include "../resources.h"

class TestScene : public np::Scene {

public:

    void setup(){
    }

	void onStart() override {
        
    };

	void loop() override {
        if( isBeginning() ){
            
        }else if( isLooping() ){
            
        }
    }

};
